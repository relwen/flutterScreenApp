import 'package:flutter/widgets.dart';

class ECCardModel {
  String? cardNo;
  String? name;
  String? img;
  IconData? icon;
  Color? col;
  Color? bgCol;
  Widget? widget;

  ECCardModel({this.cardNo, this.name, this.img, this.icon, this.col, this.bgCol, this.widget});
}
